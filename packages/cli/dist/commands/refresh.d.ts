/**
 * refresh.ts — linklab refresh [alias]
 *
 * Macro : build + generate + test + train en séquence (mode quiet).
 */
export declare function refresh(options?: {
    alias?: string;
}): Promise<void>;
//# sourceMappingURL=refresh.d.ts.map